#include <stdio.h>

int main(){
	int first,second,sum;
	printf("Sum calcuation by: Nitin\n");
	printf("Enter the two numbers: ");
	scanf("%d %d", &first, &second);
	sum = first + second;
	printf("The sum of these two numbers is: %d\n", sum);
}
